spring:
  cloud:
    config:
      enabled: false

  datasource:
    username: test
    password: test
  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true

cache:
  impl: LRU
  capacity: 20