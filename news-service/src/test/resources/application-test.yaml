spring:
  datasource:
    username: test
    password: test
  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true
  config:
    import: application-test.yaml

cache:
  impl: LRU
  capacity: 20