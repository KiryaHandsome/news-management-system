spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clevertec
    username: postgres
    password: postgres
  profiles:
    active: dev
  liquibase:
    contexts: prod
    
server:
  servlet:
    context-path: /api/v1


